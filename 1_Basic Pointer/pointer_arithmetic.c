#include <stdio.h>

int main() {

  //4. Pointer Arithmetic

  /* int arr[] = {10, 20, 30};
      int *p = arr;
      printf("%d", *(p + 1));

          // p points to arr[0] (which is 10)
          // p + 1 points to arr[1]
          // *(p + 1) gives value at arr[1] → 20
  */



  //5.
  /*
      int arr[] = {1, 2, 3, 4};
      int *p = arr;
      printf("%d %d", *p, *(p+2));     
    
      // *p gives value at arr[0] → 1
      // p + 2 points to arr[2]
      // *(p + 2) gives value at arr[2] → 3 

   */


   

  }